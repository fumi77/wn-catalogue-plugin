# ===================================
#  Column Definitions
# ===================================

columns:

    title:
        label: smart.catalogue::lang.post.title
        searchable: true

    # author:
    #   label: Author
    #   relation: user
    #   select: login
    #   searchable: true

    categories:
        label: smart.catalogue::lang.post.categories
        relation: categories
        select: name
        searchable: true
        sortable: false

    created_at:
        label: smart.catalogue::lang.post.created
        type: date
        invisible: true

    updated_at:
        label: smart.catalogue::lang.post.updated
        type: date
        invisible: true

    published_at:
        label: smart.catalogue::lang.post.published
        type: date
